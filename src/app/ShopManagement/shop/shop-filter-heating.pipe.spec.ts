import { ShopFilterHeatingPipe } from './shop-filter-heating.pipe';

describe('ShopFilterHeatingPipe', () => {
  it('create an instance', () => {
    const pipe = new ShopFilterHeatingPipe();
    expect(pipe).toBeTruthy();
  });
});
