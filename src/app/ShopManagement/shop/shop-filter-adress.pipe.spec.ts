import { ShopFilterAdressPipe } from './shop-filter-adress.pipe';

describe('ShopFilterAdressPipe', () => {
  it('create an instance', () => {
    const pipe = new ShopFilterAdressPipe();
    expect(pipe).toBeTruthy();
  });
});
