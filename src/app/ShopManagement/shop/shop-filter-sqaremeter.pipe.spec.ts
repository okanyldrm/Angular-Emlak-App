import { ShopFilterSqaremeterPipe } from './shop-filter-sqaremeter.pipe';

describe('ShopFilterSqaremeterPipe', () => {
  it('create an instance', () => {
    const pipe = new ShopFilterSqaremeterPipe();
    expect(pipe).toBeTruthy();
  });
});
