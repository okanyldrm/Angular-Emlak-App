import { ShopFilterTitlePipe } from './shop-filter-title.pipe';

describe('ShopFilterTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new ShopFilterTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
