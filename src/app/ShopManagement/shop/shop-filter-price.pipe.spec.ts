import { ShopFilterPricePipe } from './shop-filter-price.pipe';

describe('ShopFilterPricePipe', () => {
  it('create an instance', () => {
    const pipe = new ShopFilterPricePipe();
    expect(pipe).toBeTruthy();
  });
});
