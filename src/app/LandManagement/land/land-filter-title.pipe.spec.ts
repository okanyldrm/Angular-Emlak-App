import { LandFilterTitlePipe } from './land-filter-title.pipe';

describe('LandFilterTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new LandFilterTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
