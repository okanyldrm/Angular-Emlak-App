import { LandFilterReconstructionPipe } from './land-filter-reconstruction.pipe';

describe('LandFilterReconstructionPipe', () => {
  it('create an instance', () => {
    const pipe = new LandFilterReconstructionPipe();
    expect(pipe).toBeTruthy();
  });
});
