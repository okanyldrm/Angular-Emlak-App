import { LandFilterSquaremeterPipe } from './land-filter-squaremeter.pipe';

describe('LandFilterSquaremeterPipe', () => {
  it('create an instance', () => {
    const pipe = new LandFilterSquaremeterPipe();
    expect(pipe).toBeTruthy();
  });
});
