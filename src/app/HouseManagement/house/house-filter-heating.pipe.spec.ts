import { HouseFilterHeatingPipe } from './house-filter-heating.pipe';

describe('HouseFilterHeatingPipe', () => {
  it('create an instance', () => {
    const pipe = new HouseFilterHeatingPipe();
    expect(pipe).toBeTruthy();
  });
});
