import { HouseFilterRoomPipe } from './house-filter-room.pipe';

describe('HouseFilterRoomPipe', () => {
  it('create an instance', () => {
    const pipe = new HouseFilterRoomPipe();
    expect(pipe).toBeTruthy();
  });
});
