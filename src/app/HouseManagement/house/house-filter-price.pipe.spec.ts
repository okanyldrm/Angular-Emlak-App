import { HouseFilterPricePipe } from './house-filter-price.pipe';

describe('HouseFilterPricePipe', () => {
  it('create an instance', () => {
    const pipe = new HouseFilterPricePipe();
    expect(pipe).toBeTruthy();
  });
});
