import { HouseFilterAdressPipe } from './house-filter-adress.pipe';

describe('HouseFilterAdressPipe', () => {
  it('create an instance', () => {
    const pipe = new HouseFilterAdressPipe();
    expect(pipe).toBeTruthy();
  });
});
