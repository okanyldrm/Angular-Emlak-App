import { HouseFilterSaloonPipe } from './house-filter-saloon.pipe';

describe('HouseFilterSaloonPipe', () => {
  it('create an instance', () => {
    const pipe = new HouseFilterSaloonPipe();
    expect(pipe).toBeTruthy();
  });
});
